import React from 'react';
import Nav from '../components/landing/navbar'

const Landing = () => (
    
    <div>
        <Nav/>
        Fresh off the block is a blockchain based chat application
    </div>
  );
  
  export default Landing;